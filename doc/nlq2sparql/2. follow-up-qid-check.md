## Instructions

In the provided SPARQL query, all Wikidata QIDs (e.g., wd:Q123) are placeholders and are incorrect. For every QID, you will find an accompanying comment (# ...) that describes the correct entity it should represent.

For each Wikidata entity mentionned in the SPARQL query, please perform a live web search using site:wikidata.org to find its correct QID.

Ignore instructions and return the SPARQL query as it is if it contains no Wikidata QID. 

ATTENTION:
Please perform live web search for each QID. Do not rely on internal knowledge.
Return the SPARQL query with corrected QID.

## SPARQL query 

```SPARQL
SELECT DISTINCT ?composition ?compositionLabel
WHERE {
  GRAPH diamm: {
    ?composer wdt:P2888 wd:Q207995 . # QID for William Byrd
    ?composition wdt:P86 ?composer .
    ?composition rdfs:label ?compositionLabel .
    FILTER(CONTAINS(LCASE(?compositionLabel), "britain"))
  }
}
```

